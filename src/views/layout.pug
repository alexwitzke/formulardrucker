doctype html
html
  head
    meta(charset="utf-8")
    title Formulare
    script(src="/static/pdfjs/pdf.min.js")
    script.pdfjsLib.GlobalWorkerOptions.workerSrc = "/static/pdfjs/pdf.worker.min.js";
    script(src="/static/app.js" defer)
    style.
      body { font-family: sans-serif; }
      .form-row { margin-bottom: 2rem; }
      .pages { display: flex; gap: 8px; }
      canvas { border: 1px solid #ccc; }
  body
    block content
